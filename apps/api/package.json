{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "uv run ruff check .",
    "test": "uv run pytest -q",
    "typecheck": "echo 'Python: use mypy later'"
  }
}
